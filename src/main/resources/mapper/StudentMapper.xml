<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.studentsysteam.mapper.StudentMapper">
    <select id="findByCondition" resultType="com.example.studentsysteam.entity.Student">
        select s.id, s.name, s.age, s.gender, s.class_id as classId, c.class_name as className
        from student s
        left join classes c on s.class_id = c.id
        <where>
            <if test="name != null and name != ''">
                and s.name like concat('%', #{name}, '%')
            </if>
            <if test="gender != null and gender != ''">
                and s.gender = #{gender}
            </if>
        </where>
    </select>

    <delete id="deleteByIds">
        delete from student where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>

    <select id="findAllWithClass" resultType="com.example.studentsysteam.entity.Student">
        select s.id, s.name, s.age, s.gender, s.class_id as classId, c.class_name as className
        from student s
         left join classes c on s.class_id = c.id
    </select>


    <select id="findByIdWithClass" resultType="com.example.studentsysteam.entity.Student">
        select s.id, s.name, s.age, s.gender, s.class_id as classId, c.class_name as className
        from student s
        left join classes c on s.class_id = c.id
        where s.id = #{id}
    </select>

</mapper>
